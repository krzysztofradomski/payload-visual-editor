# Optional. When unset, dev and tests use an in-memory MongoDB (mongodb-memory-server).
# DATABASE_URL=mongodb://127.0.0.1/payload-plugin-template
PAYLOAD_SECRET=YOUR_SECRET_HERE

# Dev admin (seeded on startup): test@test.com / test1234
NEXT_PUBLIC_SERVER_URL=http://localhost:3000
